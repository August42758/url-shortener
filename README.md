# 🔗 URL Shortener Service

Небольшой REST API-сервис для сокращения длинных ссылок, реализованный на Go.  
Позволяет создавать короткие URL, выполнять редиректы и получать аналитику по количеству переходов.  
Поддерживает кэширование с помощью Redis и хранение данных в PostgreSQL.

---

## 🚀 Основные возможности

- **Создание коротких ссылок** — отправьте оригинальный URL, получите короткий идентификатор.  
- **Редирект по короткому URL** — автоматический переход на оригинальную ссылку.  
- **Аналитика** — получение информации о ссылке: оригинальный URL и количество переходов.  
- **Кэширование** — ускоряет редиректы, используя Redis для хранения часто используемых ссылок.  
- **Хранение данных** — все ссылки и статистика сохраняются в PostgreSQL.  

---

## 🧩 Стек технологий

- **Go (net/http)** — реализация REST API  
- **PostgreSQL** — основное хранилище ссылок  
- **Redis** — кэширование редиректов  
- **REST API** — обмен данными между клиентом и сервером  

---

## ⚙️ Эндпоинты API

| Метод | Путь | Описание |
|:------|:------|:----------|
| `POST` | `/shorten` | Создание короткой ссылки |
| `GET` | `/s/{short_url}` | Редирект по короткому URL |
| `GET` | `/analytycs/{short_url}` | Получение информации о ссылке |

---

## 🧠 Пример использования

### 1. Создание короткой ссылки
**Запрос:**
```bash
POST /shorten
Content-Type: application/json

{
  "url": "https://example.com/some/very/long/link"
}
```

**Ответ (201 Created):**
```json
{
  "short_url": "a1b2c3"
}
```

---

### 2. Редирект
**GET /s/a1b2c3**  
→ перенаправит на `https://example.com/some/very/long/link`

---

### 3. Аналитика
**GET /analytycs/a1b2c3**

**Ответ:**
```json
{
  "short_url": "a1b2c3",
  "original_url": "https://example.com/some/very/long/link",
  "redirect_count": 42
}
```

💡 Проект создан для обучения и демонстрации работы с Go, Redis и PostgreSQL.
